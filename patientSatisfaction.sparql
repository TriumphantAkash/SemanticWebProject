PREFIX DS1209: <http://logd.tw.rpi.edu/source/data-gov/dataset/1209/vocab/raw/>

SELECT distinct ?state (SUM(xsd:double(?white_outpatient)) as ?total_white_outpatient)
WHERE {  

GRAPH <http://logd.tw.rpi.edu/source/data-gov/dataset/1209/version/1st-anniversary>  {	
				?s1 DS1209:white_outpatient ?white_outpatient.
				?s1 DS1209:state ?state .   
}
}
order by ?state